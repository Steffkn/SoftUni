@model BookLibrary.Web.Models.MovieConciseViewModel

<tr>
    <td>
        <a asp-controller="Movies" asp-action="Details" asp-route-id="@Model.MovieId">
            @Model.Title
        </a>
    </td>
    <td>
        <a asp-page="/Authors/Details" asp-route-id="@Model.AuthorId">
            @Model.AuthorName
        </a>
    </td>
    <td>
        @if (Model.IsInStock)
        {
            <span class="badge badge-success">At home</span>
        }
        else
        {
            <span class="badge badge-warning">Borrowed</span>
        }
    </td>
</tr>